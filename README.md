# openGlInstall

# How To install 

    sudo apt-get update # Update central library/(อัพเดต สโตร์กลาง)
    sudo apt-get install freeglut3 freeglut3-dev binutils-gold G++ cmake libglew-dev mesa-common-dev build-essential libglew1.5-dev libglm-dev # Program && librarry /(ตัวโปรแกรมและไลบารี่ที่จำเป็น)
